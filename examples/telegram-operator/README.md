# Telegram Operator Example

This example folder documents the intended phone-first workflow:

- daemon runs locally
- Telegram bot is configured with an allowlist
- approvals can be reviewed and resolved from a phone
- `/status`, `/projects`, `/approvals`, and `/budget` provide lightweight remote visibility

It is intentionally documentation-only in v0.1.
