# Simple Node Project Example

This folder is a placeholder example target for Feather.

Suggested contents for local testing:

- a small `package.json`
- a `src/` folder
- a `.feather/project.yml` generated via `feather init`

Use it to validate:

- project registration
- git status heartbeat observations
- safe shell allowlists like `npm test`
